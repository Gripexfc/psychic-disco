## 1. API Connectivity Fix

- [x] 1.1 检查并统一前端 `VITE_API_BASE_URL`、Vite 代理与后端监听端口配置。
- [x] 1.2 校验关键接口路径与方法（`/api/site`、`/api/posts`、`/api/posts/:idOrSlug`、`POST /api/posts`）是否一致。
- [x] 1.3 修复跨域/代理导致的请求失败，确保本地联调可直接访问。
- [x] 1.4 补充接口失败可观测性（错误日志、错误提示、可重试入口）。

## 2. Homepage Reliability

- [x] 2.1 保证首页初始加载时能稳定获取站点信息与精选文章。
- [x] 2.2 在请求失败场景展示可理解的错误文案，并提供“重试”能力。
- [x] 2.3 校验首页在空数据场景下仍有可用展示，不出现白屏或断裂布局。

## 3. Article Upload Reliability

- [x] 3.1 校验发布参数完整性（标题、摘要、正文/富文本内容）并统一前后端校验规则。
- [x] 3.2 修复上传流程中的状态管理问题（防重复提交、提交中禁用、成功后跳转）。
- [x] 3.3 验证发布成功后列表与详情可立即读取新文章。
- [x] 3.4 覆盖失败场景（400/404/500）并验证错误处理不阻断后续操作。

