## 1. Backend Foundation

- [x] 1.1 初始化后端项目骨架（推荐 Node.js + TypeScript），提供统一目录结构与启动脚本。
- [x] 1.2 建立基础中间件：请求日志、参数校验、错误处理、跨域配置。
- [x] 1.3 设计统一响应格式（`data`、`meta`、`error`）和错误码约定。

## 2. Blog API

- [x] 2.1 实现文章列表接口，支持分页、支持发布文章（最小能力可先实现分页）, 数据存储在当前项目目录下, 先不需要数据库。
- [x] 2.2 实现文章详情接口，支持按 `slug` 或 `id` 查询。
- [x] 2.3 实现标签列表和站点信息接口，满足前端首页与侧边信息展示。
- [x] 2.4 提供稳定的示例数据，保证前端在无数据库依赖时也可联调。
- [x] 2.5 后端项目和前端项目同级
## 3. Frontend-Friendly Integration

- [x] 3.1 编写接口文档（参数、响应、错误示例），确保前端可独立接入。
- [x] 3.2 提供环境变量模板与本地启动说明，明确联调地址与端口规范。
- [ ] 3.3 完成一次前后端联调验证：列表、详情、错误场景（404/500）可被前端正确处理。

