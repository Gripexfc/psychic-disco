## 1. Data and API

- [ ] 1.1 扩展文章数据结构，新增点赞数与用户点赞状态字段。
- [ ] 1.2 新增点赞接口（例如 `POST /api/posts/:id/like`），支持幂等处理。
- [ ] 1.3 新增查询接口返回点赞信息，确保列表与详情可同步展示。
- [ ] 1.4 处理高频点击与重复请求，保证点赞计数一致性。

## 2. Frontend Interaction

- [ ] 2.1 在文章列表与详情页增加点赞按钮与点赞数展示。
- [ ] 2.2 点击点赞后即时反馈（按钮状态、动画、计数变化、失败提示）。
- [ ] 2.3 已点赞状态下不需要禁止重复计数 只需要延迟3秒后才能重复点击（按产品规则执行）。
- [ ] 2.4 未登录用户点赞行为按规则处理（允许游客点赞）。

## 3. Validation

- [ ] 3.1 验证点赞流程：初始状态 -> 点赞 -> 刷新后状态一致。
- [ ] 3.2 验证异常流程：接口失败、网络超时、重复点击、并发点击。
- [ ] 3.3 验证视觉与交互体验：按钮可识别、反馈及时、文案清晰。

