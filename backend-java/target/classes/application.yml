server:
  port: ${PORT:3000}

spring:
  application:
    name: blog-backend

data:
  posts-path: data/posts.json
  site-path: data/site.json

auth:
  username: goodeat
  password: goodeat123456
  token: goodeat-session-token

cors:
  frontend-origin: ${FRONTEND_ORIGIN:http://localhost:5173}
